dotnet publish .\LinguaCore.API\LinguaCore.API.csproj `
    -c Release `
    -r win-x64 `
    --self-contained true `
    -p:PublishSingleFile=true `
    -p:PublishTrimmed=false `
    -p:UseAppHost=true `
    -p:DebugType=None `
    -o publish