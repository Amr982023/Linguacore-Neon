namespace LinguaCore.Application.DTOs.Response;

public record FinancialSummaryResponse(
    decimal TotalCollected, decimal TotalExpected,
    decimal TotalCommissions, decimal NetRevenue,
    decimal TotalRefunds, decimal TotalScholarshipWaived,
    int OverdueCount, Dictionary<string, decimal> ByPaymentMethod);

public record StudentSummaryResponse(
    int TotalActive, int TotalInactive, int NewThisMonth,
    int InGracePeriod, int WithScholarship, int WithDiscount,
    int WaitingListCount, double EarlyExitRate);

public record GroupSummaryResponse(
    int TotalActive, int TotalCompleted,
    int TotalSuspended, int TotalOnline, int TotalOffline);


// ?????????????????????????????????????????????????????????????????????????
// Shared small shapes used across multiple rich dashboard responses
// ?????????????????????????????????????????????????????????????????????????
public record NameCount(string Name, int Count);
public record NameAmount(string Name, decimal Amount);
public record MonthAmount(string Month, decimal Amount);
public record YearAmount(int Year, decimal Amount);

// ?????????????????????????????????????????????????????????????????????????
// Groups — rich
// ?????????????????????????????????????????????????????????????????????????
public record GroupSummaryRichResponse(
    int TotalActive, int TotalCompleted, int TotalSuspended,
    int TotalOnline, int TotalOffline,
    IEnumerable<NameCount> ByInstructor,
    IEnumerable<NameCount> ByLevel,
    IEnumerable<NameCount> ByType,
    IEnumerable<NameCount> ByCategory,
    string PeriodLabel);

// ?????????????????????????????????????????????????????????????????????????
// Payments — rich
// ?????????????????????????????????????????????????????????????????????????
public record MonthlyTrendPoint(
    string Month, decimal Collected, decimal Outstanding,
    decimal Refunds, int OverdueCount);

public record PaymentSummaryRichResponse(
    decimal NetRevenue, decimal TotalCollected, decimal TotalExpected,
    decimal TotalRefunds, decimal TotalCommissions,
    decimal TotalDeductions,                    // ? NEW
    decimal CashInDrawerAfterPeriod,             // ? NEW: income - deductions - refunds - net commission, for THIS period only
    int OverdueCount,
    Dictionary<string, decimal> ByPaymentMethod,
    IEnumerable<NameAmount> ByGroup,
    IEnumerable<NameAmount> ByClosingType,
    IEnumerable<MonthAmount> ByMonthYTD,
    IEnumerable<YearAmount> ByYear,
    IEnumerable<MonthlyTrendPoint> MonthlyTrend,
    string PeriodLabel);

// ?????????????????????????????????????????????????????????????????????????
// Instructors — rich
// ?????????????????????????????????????????????????????????????????????????
public record InstructorSessionStat(string Name, int Sessions, decimal Commission);
public record InstructorSessionCount(string Name, int Sessions);

public record InstructorSummaryRichResponse(
    decimal TotalCommissions, decimal NetRevenue, int PendingClosings,
    IEnumerable<InstructorSessionStat> InstructorMonthly,
    IEnumerable<InstructorSessionCount> InstructorClosing,
    string PeriodLabel);

// ?????????????????????????????????????????????????????????????????????????
// Exams — rich
// ?????????????????????????????????????????????????????????????????????????
public record GroupPassCount(string Name, int Passed);
public record StudentPassRate(string Name, double Rate);

public record ExamSummaryRichResponse(
    int ExamsThisMonth, double? AvgPassRate, int FailedFinals, int CertificatesIssued,
    IEnumerable<NameCount> ExamsByType,
    IEnumerable<NameCount> ExamsByResult,
    IEnumerable<GroupPassCount> GroupExamRank,
    IEnumerable<StudentPassRate> StudentPassRate,
    string PeriodLabel);

// ?????????????????????????????????????????????????????????????????????????
// Waiting list — rich
// ?????????????????????????????????????????????????????????????????????????
public record WaitingDayCount(int Days, int Count);
public record WaitingBucket(string Label, int Count);

public record WaitingSummaryRichResponse(
    int WaitingListCount, int EnrolledFromWaiting, int WaitingCancelled, double AvgWaitDays,
    IEnumerable<WaitingDayCount> WaitingByDays,
    IEnumerable<WaitingBucket> WaitingBuckets,
    string PeriodLabel);



public record CashDrawerResponse(
    decimal TotalIncomeAllTime,
    decimal TotalRefundsAllTime,
    decimal TotalCenterDeductionsAllTime,
    decimal TotalCommissionEarnedAllTime,
    decimal TotalInstructorBonusesAllTime,
    decimal TotalInstructorSalaryDeductionsAllTime,
    decimal CashInDrawer,
    decimal PendingCommissionNotYetClosed,
    int OutstandingPaymentsCount,
    DateTime AsOf);

